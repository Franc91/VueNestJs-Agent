---
description: Multi-layer Vue + NestJS work — new screen, API+UI, store, Colada, grid. One row in routing.md, then one skill. Skip supplementary and review unless Substantial. Polish — nowy ekran, endpoint, store, lista.
alwaysApply: false
---

# Agent orchestration

**Multi-layer** task in `ui/` or `src/` (not a one-file edit):

1. [conventions cache](../cache/conventions.md) once, then [routing.md](../specialists/routing.md) — one row, one skill at a time.
2. Skip `.agents/skills`, review, and perf unless the user asked or the task is Substantial.
