# nestjs-best-practices

**Path:** `.agents/skills/nestjs-best-practices/SKILL.md`

**Read when:** [Backend tier → Standard or Substantial](../backend-tier.md); `data-grid-agent` backend metadata; module/DI/guards/validation issues.

**Pair with:** `nestjs-api-agent`, `data-grid-agent`

**Active rules:** [stack-profile.md → Backend](../../stack-profile.md#backend-stack) — `arch-*`, `di-*`, `error-*`, selected `security-*`, `perf-*`, `db-*`, `api-*`, `test-*`

Load individual files from `.agents/skills/nestjs-best-practices/rules/` for active prefixes.

**Inactive:** `micro-*`, `security-auth-jwt`, `api-versioning`, most `devops-*` — see stack-profile.

**Install:** [install.md](install.md#nestjs-best-practices)
